# Agent Runs

Each worker writes one independent run log here.
The logs must capture:
- what the worker could freeze cleanly
- where ambiguity remained
- whether the blocker felt like a skill gap, public-surface gap, code gap, or task ambiguity
