name: rustplc-no-board-gate

on:
  push:
  pull_request:

jobs:
  no-board-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Project check
        run: cargo run --release --bin rust_plc -- project-check plc/deliveries/line/plc_gen_megapipeline/plc/main.bundle.toml --scenario plc/deliveries/line/plc_gen_megapipeline/scenarios/nominal/normal.yaml --out-dir out/project_check/normal --output json
