{
  "project": "RustPLC",
  "branchName": "ralph/dsl-control-rust-compute",
  "description": "Implement the hybrid architecture where DSL keeps deterministic control logic and complex numerical computation is executed by Rust extern functions with contract checks.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Freeze extern-function MVP syntax and scope",
      "description": "As a language designer, I want a finalized MVP syntax spec so that parser/runtime work can proceed without ambiguity.",
      "acceptanceCriteria": [
        "Document supported MVP syntax for `extern function` declaration and `action: call` invocation",
        "Mark unsupported items explicitly (for example: overloads, variadic args, generics, callbacks)",
        "Provide at least 10 valid/invalid DSL examples in docs",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "MVP syntax spec frozen in docs/extern_function_mvp_spec.md"
    },
    {
      "id": "US-002",
      "title": "Parser support for extern function declarations",
      "description": "As a compiler engineer, I want the DSL parser to read extern declarations so that contract metadata is available in the AST.",
      "acceptanceCriteria": [
        "Extend `plc.pest` to parse `extern function <name>(...) -> ... { ... }` inside `[topology]`",
        "Parse required contract fields: `rust_module`, `pure`, `time_bound_us`",
        "Reject missing required fields with line-aware parse errors",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Parser now supports extern function declarations with required contract field validation and AST capture"
    },
    {
      "id": "US-003",
      "title": "Parser support for extern call actions",
      "description": "As a DSL user, I want to call extern functions in task steps so that complex math can execute outside expression grammar.",
      "acceptanceCriteria": [
        "Parse `action: call func(a, b) -> out_var`",
        "Parse tuple return binding form `-> (a, b, c)`",
        "Restrict extern calls to action context and reject expression-context usage",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Parser now supports action: call extern invocations (single/tuple bindings) and rejects extern use in expression context"
    },
    {
      "id": "US-004",
      "title": "AST and IR extensions for extern metadata and call actions",
      "description": "As a compiler maintainer, I want explicit AST/IR nodes for extern functions and calls so that semantic checks and runtime lowering are type-safe.",
      "acceptanceCriteria": [
        "Add `ExternFunctionDeclaration` and related param/return/constraint types to AST",
        "Add runtime action variant for extern invocation with argument and return-variable bindings",
        "Serde roundtrip for new AST nodes succeeds",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "IR now carries extern function metadata and call_extern actions with binding info; AST/IR serde+lowering coverage added"
    },
    {
      "id": "US-005",
      "title": "Semantic validation for function declaration and call signatures",
      "description": "As a verification engineer, I want compile-time signature checks so that invalid extern calls fail before runtime.",
      "acceptanceCriteria": [
        "Collect extern function symbols with unique names during semantic analysis",
        "Validate call argument count and argument types against declaration",
        "Validate return binding arity/types for single and tuple returns",
        "Emit diagnostics with function name and source line",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Semantic analysis now builds an extern signature table and validates call arg count/types plus return binding arity/types with line-aware diagnostics"
    },
    {
      "id": "US-006",
      "title": "Compile-time contract checks for determinism constraints",
      "description": "As a safety engineer, I want static contract checks so that nondeterministic or unsafe extern declarations are rejected early.",
      "acceptanceCriteria": [
        "Reject non-positive `time_bound_us` values",
        "Reject missing or empty `rust_module`",
        "Reject unsupported type usage in extern signatures for Phase 1 scalar-only scope",
        "Add diagnostics for duplicate function names (no overloads)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Semantic analysis now enforces extern contract constraints (non-empty rust_module, positive time_bound_us, Phase-1 scalar signature guard) with line-aware diagnostics"
    },
    {
      "id": "US-007",
      "title": "Implement ExternFunctionRegistry core",
      "description": "As a runtime engineer, I want a registry abstraction so that extern calls resolve to Rust implementations with metadata.",
      "acceptanceCriteria": [
        "Add registry API: register/get/call for extern functions",
        "Define structured runtime errors (`InvalidArgCount`, range errors, timeout, runtime error)",
        "Ensure missing-function lookup returns deterministic error",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Added ExternFunctionRegistry core API (register/get/call) with deterministic missing-function behavior and structured runtime errors for arg count, range, timeout, and runtime failures"
    },
    {
      "id": "US-008",
      "title": "Integrate extern call execution into runtime tick",
      "description": "As a controls runtime user, I want extern calls to execute within step actions so that results can drive subsequent control logic.",
      "acceptanceCriteria": [
        "Handle `Action::CallExtern` in runtime execution path",
        "Map variable indices to argument list and write returned values back to bound variables",
        "Propagate registry errors as runtime errors with function context",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Runtime bridge now lowers call_extern into runtime-core Action::CallExtern; runtime tick executes externs via tick_with_extern APIs and propagates registry errors with function context"
    },
    {
      "id": "US-009",
      "title": "Deliver built-in math externs (add/multiply/quadratic_fit)",
      "description": "As an application engineer, I want baseline math externs available so that the hybrid architecture can be validated with real workloads.",
      "acceptanceCriteria": [
        "Implement `add` and `multiply` extern functions",
        "Implement `quadratic_fit` extern function and singular-matrix error path",
        "Register these built-ins in registry bootstrap",
        "Add unit tests for happy path and invalid argument counts",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Added registry bootstrapped built-in math externs (add/multiply/quadratic_fit) with quadratic singular-matrix runtime error path and unit coverage for happy path + invalid arg counts"
    },
    {
      "id": "US-010",
      "title": "Deliver stateful PID extern function",
      "description": "As a controls engineer, I want a non-pure PID extern implementation so that closed-loop control scenarios can run through the new call path.",
      "acceptanceCriteria": [
        "Implement `pid_update(error, kp, ki, kd, dt) -> output` with internal state",
        "Provide a deterministic reset path for tests",
        "Add unit tests covering first-step response and multi-step accumulation",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Added built-in non-pure `pid_update(error, kp, ki, kd, dt)` with registry-local state and `reset_builtin_pid_state()` for deterministic tests"
    },
    {
      "id": "US-011",
      "title": "Runtime contract enforcement for range and timeout",
      "description": "As a reliability engineer, I want runtime guardrails so that extern function violations are caught and reported deterministically.",
      "acceptanceCriteria": [
        "Validate input ranges before function invocation when `input_range` is configured",
        "Validate output ranges after function invocation when `output_range` is configured",
        "Measure call duration via host timing hook and enforce `time_bound_us`",
        "Return explicit timeout/range errors with offending index and value",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Extern runtime contract errors now covered end-to-end: runtime tick extern path propagates input/output range and timeout violations with explicit offending index/value details."
    },
    {
      "id": "US-012",
      "title": "Compile-time tick budget and non-pure conflict checks",
      "description": "As a system verifier, I want scheduler-level checks so that total extern cost and non-pure call conflicts are detected before deployment.",
      "acceptanceCriteria": [
        "Compute per-tick worst-case sum of extern `time_bound_us` values",
        "Fail compilation when extern budget exceeds configured tick budget",
        "Detect and report unsafe concurrent usage patterns for non-pure externs",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Added semantic non-pure extern concurrency checks for parallel/race branches and runtime-bridge tick-budget enforcement that rejects programs when worst-case per-tick extern `time_bound_us` exceeds configured `tick_ms` budget."
    },
    {
      "id": "US-013",
      "title": "Extend causality verification to include extern calls",
      "description": "As a formal verification user, I want extern call edges in causality analysis so that control-plane safety guarantees remain intact.",
      "acceptanceCriteria": [
        "Model extern call nodes in causality graph",
        "Treat `pure` extern functions as deterministic transforms in propagation checks",
        "Add positive and negative causality tests covering extern call chains",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Causality now models extern call nodes by wiring call-site argument dependencies into the graph, propagates only through `pure: true` extern transforms, and includes positive/negative verification tests for extern call chains."
    },
    {
      "id": "US-014",
      "title": "Standardize extern error handling in DSL flow",
      "description": "As a PLC developer, I want a clear error reporting pattern so that call failures can be handled with explicit control logic.",
      "acceptanceCriteria": [
        "Implement runtime error code surface for extern failures (for example `last_error` mapping)",
        "Document and test explicit branching pattern for retry/fallback/error tasks",
        "Keep syntax backward-compatible without introducing mandatory new control keywords",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Added extern error-code capture flow via runtime-core tick_with_extern_error_code + stable ExternRuntimeError code mapping for last_error branching/retry/fallback patterns"
    },
    {
      "id": "US-015",
      "title": "End-to-end integration tests for compile-to-runtime extern flow",
      "description": "As a release owner, I want full integration coverage so that extern features are verified from DSL source to runtime state updates.",
      "acceptanceCriteria": [
        "Add integration test for quadratic fitting DSL program with tuple return binding",
        "Add integration test for PID control loop using extern output in subsequent actions",
        "Add integration test for at least one runtime error path (range/timeout/runtime failure)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 15,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Performance benchmark and regression gate for extern calls",
      "description": "As a performance owner, I want benchmark gates so that extern integration remains within real-time overhead limits.",
      "acceptanceCriteria": [
        "Add benchmark for simple extern call overhead and complex extern call overhead",
        "Record baseline and compare against regression branch in CI or script gate",
        "Fail gate when overhead exceeds documented thresholds",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 16,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Developer tooling for extern registration and declaration generation",
      "description": "As a library author, I want helper tooling so that new extern functions can be added quickly and consistently.",
      "acceptanceCriteria": [
        "Provide `register_extern_function!`-style macro or equivalent helper API",
        "Generate DSL extern declaration stubs from Rust registration metadata",
        "Add tests for generated declaration consistency with runtime signatures",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 17,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Documentation and examples for hybrid architecture rollout",
      "description": "As a user adopting the feature, I want complete docs and examples so that I can safely implement extern-based control programs.",
      "acceptanceCriteria": [
        "Publish extern function development guide and contract design best practices",
        "Add at least 10 practical DSL examples including PID and fitting scenarios",
        "Document migration guidance and known limitations for current phase",
        "Typecheck passes"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    }
  ]
}
