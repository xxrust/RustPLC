# Generated by `rust_plc scenario-init`.
# Source PLC: main.plc
# Preset: timeout
# Keep `at_ms` aligned to `tick_ms`, and keep `at_ms` < `duration_ms`.
tick_ms: 10
duration_ms: 2000

# No physical X*/AI* inputs were discovered from this PLC topology.
inputs:
  - at_ms: 0
    set:
      digital_inputs:
        0: true  # (example) press start button
  - at_ms: 50
    set:
      digital_inputs:
        0: false # (example) release

# Force/override (optional). Use YAML `null` to clear a forced value.
# Example:
# forces:
#   - at_ms: 0
#     set:
#       digital_inputs:
#         0: true
#   - at_ms: 100
#     set:
#       digital_inputs:
#         0: null
forces: []
