# Source PLC: examples/stepper_collision_guard.plc
seed: 2026
tick_ms: 10
duration_ms: 2500

inputs:
  - at_ms: 0
    set:
      digital_inputs:
        alarm_x: false
        enc_dir_positive: false
        inpos_x: false
      analog_inputs:
        zone_code: 1
        axis_count: 0

  - at_ms: 100
    set:
      analog_inputs:
        zone_code: 0

  - at_ms: 150
    set:
      digital_inputs:
        enc_dir_positive: true

  - at_ms: 900
    set:
      analog_inputs:
        axis_count: 125000

  - at_ms: 1000
    set:
      digital_inputs:
        inpos_x: true
