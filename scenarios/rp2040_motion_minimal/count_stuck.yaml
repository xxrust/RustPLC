# Source PLC: examples/rp2040_motion_minimal.plc
seed: 2026
tick_ms: 10
duration_ms: 2500

inputs:
  - at_ms: 0
    set:
      digital_inputs:
        axis0_enc_dir_positive: true
        axis1_enc_dir_positive: true
      analog_inputs:
        axis0_count: 0
        axis1_count: 0

  # Counts stay stuck at 0 -> should timeout in wait_axis0_count.

