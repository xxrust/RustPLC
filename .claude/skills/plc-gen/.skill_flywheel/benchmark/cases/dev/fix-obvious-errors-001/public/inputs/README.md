# Public Inputs

Use `feed_cassette_excerpt.plc` as the only PLC source input for this case.

The runner should apply the `plc-gen` `fix` workflow and report obvious semantic errors before proposing repairs.
