• 这版已经很接近可执行了，但我还看到 3 个需要再修的小坑：

  - 🔴 action_to_transition_action 示例签名不一致：文档里写了 return Err(...) / Ok(...)（docs/motor-control-
    enhancement.md:155），但当前函数签名是返回 TransitionAction 不是 Result（src/semantic/mod.rs:2957），而且示例里用到
    的 line 变量在该函数也不存在。
  - 🟠 normalize_motor_compat 伪代码里遍历任务的写法会误导实现：for task in &mut program.tasks（docs/motor-control-
    enhancement.md:343）不匹配当前 AST 结构，TasksSection 实际是 tasks: Vec<TaskDeclaration>（src/ast/mod.rs:261）。
  - 🟠 “改写所有 StateReference”的覆盖范围不完整：文档声称会统一改写（docs/motor-control-enhancement.md:324），但示例只
    处理了 constraints.safety 和 tasks（docs/motor-control-enhancement.md:336），漏了 causality 链里的
    StateReference（src/ast/mod.rs:253）。

  总体上方向没问题；把这 3 处补齐后，这份文档就能更稳地指导落地实现。