# Ralph Progress Log
## Codebase Patterns
- When the repo root is both `[package]` and `[workspace]`, use `cargo test --workspace` to run member-crate tests (plain `cargo test` only targets the default package).
- For `#![no_std]` crates that still want unit tests/doctests, add `#[cfg(test)] extern crate std;`.
- In `runtime-core`, `delay`/`timeout` are evaluated as `elapsed = now_tick - step_entered_tick`, and they fire when `elapsed >= configured_ticks` (so `after_ticks: 0` is immediate on the entry tick).
- In `crates/sim::SimIo`, input changes scheduled for tick `N` are applied when `advance_tick()` moves to `N`, so they are visible during the runtime's tick-`N` evaluation (tick-0 changes are applied in `SimIo::new`).
- For `crates/sim` plant-style dynamics, keep the plant as a deterministic tick-step that *returns* input updates, and call it from `SimIo::advance_tick()` (after applying scenario inputs) to avoid borrow-checker pitfalls with `&mut self` callbacks.
- Waveform exports are deterministic by construction: VCD assigns idcodes in a fixed order (`di<N>` then `do<N>`) and uses timestamps `tick * tick_ms` with `$timescale 1ms`.
- For human-friendly YAML parse errors, use `serde_path_to_error` so errors include a field path (e.g., `inputs[0].set.digital_inputs`).
- `serde_yaml` enum representations can be surprising (it expects YAML tags like `!Variant`); for author-friendly scenario files prefer struct-shaped keys like `faults: - sensor_stuck: { at_ms, target, value }`.
- `src/runtime_bridge.rs` builds `runtime-core::Program<'static>` by leaking boxed slices/strings (`Box::leak`) to keep the runtime API simple for now; treat this as OK for CLI/tests but avoid in long-lived processes.
- The repo is not currently rustfmt-clean; treat `cargo fmt --check` as non-gating unless you are intentionally doing a formatting pass.
- CLI integration tests in this repo should invoke the compiled binary via `env!("CARGO_BIN_EXE_rust_plc")` and verify artifacts on disk.
- Batch SIL regress runs live in `rust_plc::sim_regress`: it scans `.plc`/scenario YAMLs recursively, writes per-case `trace.jsonl`/`report.json` under `out/sim-regress/case_XXXX`, and emits a summary JSON.
- For embedded workspace crates, keep a host-stub `main()` behind `cfg(not(target_os = "none"))` so `cargo test --workspace` stays green while firmware code is `cfg(target_os = "none")`.

Started: Sat Feb 14 23:50:11 CST 2026
---
## [2026-02-15 01:46:15 CST] - US-010
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `sim-regress` subcommand that scans a PLC directory + scenario directory, runs all combinations via the existing compiler->runtime bridge, and writes a JSON summary (pass/fail counts + failures with repro params and artifact paths).
  - Added `rust_plc::sim_regress::run_sim_regress()` as a reusable entrypoint; per-case artifacts include `trace.jsonl` + `report.json`.
  - Added an integration test covering one pass + one timeout failure and asserting summary counts + failure list.
- Files changed
  - `src/sim_regress.rs`, `src/lib.rs`, `src/main.rs`, `tests/sim_regress.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For SIL batching, keep the directory scan + run loop deterministic by sorting paths and using stable `case_XXXX` artifact folders.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `SimIo` must be sized to cover the maximum I/O IDs referenced by both the bridged `Program` and the scenario input/fault scripts; otherwise reads default to `false` and can change behavior.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `sim::run_program_for_scenario()` already produces a `SimReport` with the first timeout captured as a structured failure; `sim-regress` just aggregates those reports.
---

## [2026-02-15 01:34:30 CST] - US-009
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Extended scenario YAML to support `faults` with `sensor_stuck` (target/value/at_ms) and applied it via `SimIo::schedule_sensor_stuck`.
  - Added `sim` report generation (`SimReport`) that records the first timeout as a structured failure (kind/message/at_ms/task/step) and includes seed + scenario summary.
  - Updated `rust_plc sim` to write `report.json` (and added a CLI integration assertion), plus unit tests covering the stuck-sensor timeout case.
- Files changed
  - `crates/sim/src/lib.rs`, `crates/sim/src/scenario.rs`, `crates/sim/src/report.rs`, `crates/sim/src/runner.rs`, `src/main.rs`, `tests/sim_cli.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - `serde_yaml` expects YAML tags for enums; for author-friendly config files, model "kinds" as explicit mapping keys (e.g., `sensor_stuck: { ... }`) instead of enums.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Apply faults after scenario inputs/plant updates for the tick so stuck values deterministically override normal behavior.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `sim::run_program_for_scenario()` is a convenient single entrypoint for CLI/tests to get both JSONL trace and `SimReport`.
---

## [2026-02-15 20:57:53 CST] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added new `crates/codegen` crate with `generate_program_module()` to emit a `'static` `runtime_core::Program` module as plain Rust source (static arrays only, no heap allocations).
  - Implemented minimal validation for referenced `StepId`s so generated code doesn't index out of bounds at runtime.
  - Added a unit test that generates a tiny Program and verifies the generated source parses via `syn`.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `crates/codegen/Cargo.toml`, `crates/codegen/src/lib.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Codegen can stay dependency-light by outputting plain Rust and validating it via `syn::parse_file()` in tests.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - When formatting numeric literals for generated code, keep `f32` values explicit (`1.0`, `f32::NAN`) to avoid type inference surprises.
---

## [2026-02-15 21:03:20 CST] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `build-rp2040` CLI subcommand that compiles + verifies `.plc`, bridges to `runtime-core::Program`, runs codegen, and emits build artifacts.
  - The command now writes `generated_program.rs`, `build_meta.json`, and `io_map.template.toml` into `--out <dir>`.
  - Added build metadata fields required by PRD: `plc_sha256`, `generated_at` (RFC3339), `tool_version`, and `runtime_semver`.
  - Added integration test `tests/build_rp2040.rs` to validate successful artifact generation and metadata shape.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `crates/runtime-core/src/lib.rs`, `src/main.rs`, `tests/build_rp2040.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Reusing `compile_pipeline` + `runtime_bridge` keeps CLI build artifacts aligned with the same semantic/verification path used by existing commands.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - For stable build metadata, expose runtime version from `runtime-core` (`pub const VERSION`) instead of duplicating literals in CLI code.
---

## [2026-02-15 21:17:00 CST] - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Updated `crates/board-rp2040` to execute a generated (or default) `runtime-core::Program` via `Runtime::tick_with_trace()` instead of a hardcoded tick counter loop.
  - Added build-time injection of `generated_program.rs` via `RUST_PLC_GENERATED_PROGRAM_RS` (copied into OUT_DIR by `build.rs` and `include!()`'d by firmware).
  - Kept host-stub build path intact and added a hint for the injection environment variable.
- Files changed
  - `crates/board-rp2040/Cargo.toml`, `crates/board-rp2040/build.rs`, `crates/board-rp2040/src/main.rs`, `crates/codegen/src/lib.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Prefer outer attributes (`#[allow(...)]`) in generated files so they can be safely `include!()`'d inside other modules/crates (inner `#![...]` may fail).
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `include!()`-d sources are not crate roots; inner attributes like `#![allow(...)]` are rejected unless they annotate the enclosing module.
---

## [2026-02-15 21:21:42 CST] - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Defined RP2040 digital I/O map contract as TOML: `[digital_inputs] di<N>=<gpio>` and `[digital_outputs] do<N>=<gpio>`.
  - Added `rust_plc::io_map::IoMap` with TOML parsing + validation (duplicate GPIO, illegal range 0..=29, missing required ids).
  - Extended `build-rp2040` with optional `--io-map <file>`; when provided, it validates against the generated Program's referenced DI/DO ids and embeds the parsed map into `build_meta.json`.
  - Added unit tests covering parse/validate success and common failure cases.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `src/lib.rs`, `src/io_map.rs`, `src/main.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep mapping files author-friendly by using stable keys (`di0`, `do0`) and validating them against actual Program IO usage at build time.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `toml::Value` parsing keeps error messages clear without inventing custom deserializers for key prefixes.
---

## [2026-02-15 21:32:22 CST] - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added RP2040 `PicoIo` in `board-rp2040` implementing `io_traits::Io` digital I/O methods and tick progression.
  - Added compile-time IO map embedding in `board-rp2040/build.rs` via optional `RUST_PLC_IO_MAP_TOML` (fallback to default empty map).
  - Firmware now initializes pin directions from embedded map (`DI -> SIO input`, `DO -> SIO output`) and drives runtime ticks with trace logging.
  - Added startup/tick/trace logs in firmware loop to satisfy minimal board observability requirements for runtime step transitions.
- Files changed
  - `crates/board-rp2040/Cargo.toml`, `crates/board-rp2040/build.rs`, `crates/board-rp2040/src/main.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For dynamic RP2040 pin routing, convert typed board pins to dynamic pins once, then apply function/pull/OE at runtime from generated config.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `rp-pico` board aliases are not always `gpioNN` field names (`led`, `b_power_save`, `vbus_detect`, `voltage_monitor`); mapping code must use board-specific names.
---

## [2026-02-15 21:35:13 CST] - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `flash-rp2040` CLI subcommand to copy a UF2 image onto a user-provided mount directory.
  - Implemented `--dry-run` mode to preview the copy action without writing.
  - Added integration tests using a temp directory as a simulated mount point (copy + dry-run behaviors).
- Files changed
  - `src/main.rs`, `tests/flash_rp2040.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep flashing automation explicit and testable by requiring `--mount <path>` instead of trying to auto-detect OS-specific RP2040 volumes.
---

## [2026-02-15 21:39:55 CST] - US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Standardized board trace line format in `board-rp2040` firmware logs:
    - `TICK tick=<...> ts_ms=<...>`
    - `TRACE tick=<...> task=<...> from=<...> to=<...> reason=<...> ts_ms=<...>`
  - Added host-side parser module `rust_plc::board_trace` to convert text logs into structured rows.
  - Added `trace-parse` CLI subcommand to read board logs and emit JSONL structured trace events.
  - Added parser unit test and CLI integration test for deterministic conversion behavior.
- Files changed
  - `crates/board-rp2040/src/main.rs`, `src/lib.rs`, `src/board_trace.rs`, `src/main.rs`, `tests/trace_parse.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep board trace messages key-value structured from day one; host-side tooling then only needs lightweight parsing and can evolve independently.
---

## [2026-02-15 00:16:45 CST] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added new `crates/runtime-core` crate implementing a minimal deterministic tick-driven executor.
  - Implemented a small Program/Task/Step model with `Action`, `WaitDigital` (with optional timeout->goto), `Delay`, `Goto`, and `Halt`.
  - Added unit tests covering delay/timeout boundary semantics and same-tick goto chaining with trace events.
- Files changed
  - `Cargo.toml`, `crates/runtime-core/Cargo.toml`, `crates/runtime-core/src/lib.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Tick semantics for `Delay`/`Timeout` should be expressed in terms of `elapsed = now - entered_at` to keep behavior deterministic and easy to test.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Within a single tick, immediate transitions can loop forever if you build a goto-cycle; `runtime-core` guards with a per-tick transition cap.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `Runtime::tick*()` advances `Io` by exactly one tick (`Io::advance_tick()`), so callers should not advance ticks separately.
---

## [2026-02-15 00:07:11 CST] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added a Cargo workspace at the repo root (without migrating the existing compiler crate).
  - Added new `crates/io-traits` crate with a minimal, no_std-friendly `Io` trait (digital/analog in/out + tick).
  - Added a doctest and a unit test demonstrating a minimal in-memory `Io` implementation.
- Files changed
  - `Cargo.toml`, `crates/io-traits/Cargo.toml`, `crates/io-traits/src/lib.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Workspaces that also have a root package may require explicit `--workspace` when you want member crates covered by tests.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - `cargo clippy` isn't installed in the default toolchain here; use `cargo check`/`cargo test` unless you install it via `rustup component add clippy`.
  - Useful context (e.g., "the evaluation panel is in component X")
    - The new IO surface is intentionally tick-based (not wall-clock) to keep deterministic runtimes platform-agnostic.
---

## [2026-02-15 00:23:16 CST] - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added new `crates/sim` crate with `SimIo` (scriptable digital/analog inputs by tick) and output edge recording.
  - Added minimal JSONL trace recording via `JsonlTraceRecorder` for `runtime-core` transition events.
  - Added a unit test driving a minimal `runtime-core` Program with `SimIo`, asserting output edges and trace event order/count.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `crates/sim/Cargo.toml`, `crates/sim/src/lib.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Applying scheduled input changes in `Io::advance_tick()` is a clean way to make “tick N change is visible during tick N evaluation” without mutating inside `Io::tick()`.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - If you want a scripted input to be visible at tick 0, you must apply it at construction time (or before the first `Runtime::tick*()` call); later application happens on `advance_tick()`.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `JsonlTraceRecorder` records one JSON object per transition; use `into_string()` to write it as newline-terminated JSONL.
---

## [2026-02-15 00:30:56 CST] - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added YAML scenario parsing in `crates/sim` (`Scenario`: seed/tick_ms/duration_ms + inputs at_ms/set).
  - Implemented deterministic ms->tick mapping + `Scenario::apply_to_simio()` to schedule scripted input changes onto `SimIo`.
  - Added a unit test verifying scripted input timing and trace/output determinism (reproducible run-to-run).
- Files changed
  - `crates/sim/Cargo.toml`, `crates/sim/src/lib.rs`, `crates/sim/src/scenario.rs`, `Cargo.lock`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - `serde_path_to_error` + `serde_yaml::Deserializer` yields YAML errors with a field path, which is good for scenario authoring UX.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - If `at_ms` is not aligned to `tick_ms`, you must choose rounding semantics; this implementation validates alignment to avoid silent shifts.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `Scenario::apply_to_simio()` schedules tick-based changes so they follow `SimIo` semantics (changes at tick N are visible during tick N evaluation).
---

## [2026-02-15 00:39:12 CST] - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Extended the root `rust_plc` CLI to support a `sim` subcommand while keeping the original `rust_plc <file.plc>` behavior unchanged.
  - Implemented `rust_plc sim <scenario.yaml> [--out <trace.jsonl>]` to run a tiny `runtime-core` Program with `sim::SimIo` and write a JSONL trace (via `sim::JsonlTraceRecorder`).
  - Added an integration test that runs the compiled `rust_plc` binary and asserts the trace file exists and is non-empty.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `src/main.rs`, `tests/sim_cli.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Integration tests can run the compiled CLI via `env!("CARGO_BIN_EXE_rust_plc")` and validate file outputs without adding extra test deps.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - When the root is both `[package]` and `[workspace]`, remember to run `cargo test --workspace` so member-crate tests are included.
---

## [2026-02-15 00:57:00 CST] - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `rust_plc::runtime_bridge` to convert a compiled IR `StateMachine` into a minimal `runtime-core` `Program` (subset: set/extend/retract, wait, delay, timeout->goto, goto).
  - Implemented deterministic topology-based resolution from device names to physical `Xn`/`Yn` IDs by walking upstream `connected_to` edges.
  - Added integration tests that compile a small `.plc` fixture, bridge it, run it with `sim::SimIo`, and assert deterministic JSONL trace + output edges (including timeout branch).
- Files changed
  - `src/lib.rs`, `src/runtime_bridge.rs`, `tests/runtime_bridge_us006.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Bridging from IR to `runtime-core` is easiest when you normalize durations to ticks (`duration_ms % tick_ms == 0`) and keep guard parsing intentionally strict.
    - Mapping “logical” devices (cylinder/sensor/button) to physical I/O can be done by traversing upstream edges in `TopologyGraph` and selecting a unique `Xn`/`Yn` terminal.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `runtime-core::Runtime<'a>` ties its lifetime to the borrowed `Program`, so test helpers should avoid forcing `Runtime<'static>` unless you truly have a `'static` borrow.
  - Useful context (e.g., "the evaluation panel is in component X")
    - This bridge currently returns `Program<'static>` by leaking allocations (`Box::leak`) to avoid self-referential owned program plumbing; revisit if you need long-running processes.
---

## [2026-02-15 01:07:22 CST] - US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added deterministic waveform exports in `crates/sim`: VCD for digital inputs/outputs, plus CSV/JSONL exporters for analog output edges.
  - Extended `rust_plc sim` to write `wave.vcd` and `analog.csv` alongside the trace (paths configurable via new flags).
  - Added tests covering VCD contents (signal names + at least one edge) and CLI artifact generation.
- Files changed
  - `crates/sim/src/lib.rs`, `crates/sim/src/waveform.rs`, `src/main.rs`, `tests/sim_cli.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep waveform output deterministic: stable signal ordering + stable event sorting (time, then signal index) makes regression diffs reliable.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - If you derive output directories from `--out`, also ensure you `create_dir_all()` for each artifact path (trace/VCD/CSV) to avoid partial output failures.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `SimIo` now records digital input edges when scheduled changes are applied, which makes it possible to export DI waveforms without sampling every tick.
---

## [2026-02-15 01:19:50 CST] - US-008
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added a minimal deterministic plant model in `crates/sim` (solenoid valve -> cylinder -> limit sensor) with response time, stroke/retract time, and sensor debounce.
  - Integrated the plant into `SimIo` so sensor inputs update automatically on `advance_tick()`.
  - Added a unit test that drives a tiny `runtime-core` program and asserts the wait is satisfied at the expected tick window (response + stroke).
- Files changed
  - `crates/sim/src/lib.rs`, `crates/sim/src/plant.rs`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Returning `Vec<DigitalInputUpdate>` from the plant step avoids borrow-checker issues compared to passing `&mut SimIo` callbacks.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Avoid iterating `self.scheduled.get()` while mutating inputs; `remove()` the tick's entry to take ownership of changes.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Plant observes final output values at the end of `prev_tick` and models command changes as happening on `prev_tick`; response time is `prev_tick + response_ticks`.
---

## [2026-02-15 20:37:48 CST] - US-011
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added new `crates/board-rp2040` no_std firmware crate for Raspberry Pi Pico (RP2040), with `thumbv6m-none-eabi` entrypoint, clock init, RTT defmt logging, and a minimal tick loop.
  - Added host-stub build path (`cfg(not(target_os = "none"))`) so workspace checks/tests continue to pass on non-embedded hosts.
  - Added linker/runtime support files: crate-level `memory.x`, `build.rs` linker search setup, and workspace `.cargo/config.toml` target rustflags (`link.x` + `defmt.x`).
  - Added build guide doc at `docs/board_rp2040.md` with exact `rustup target add` and `cargo build -p board-rp2040 --target thumbv6m-none-eabi` steps.
- Files changed
  - `Cargo.toml`, `Cargo.lock`, `.cargo/config.toml`, `crates/board-rp2040/Cargo.toml`, `crates/board-rp2040/build.rs`, `crates/board-rp2040/memory.x`, `crates/board-rp2040/src/main.rs`, `docs/board_rp2040.md`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Embedded crates inside this workspace should provide a host fallback entrypoint so `cargo check/test --workspace` doesn't require cross-target compilation.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `rp-pico` clock frequency helpers are trait-based; for `get_freq()` you must import `rp_pico::hal::clocks::ClockSource` explicitly.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Current tick source is a busy-wait skeleton (`cortex_m::asm::delay`) intended only as a minimal bring-up baseline; later stories can switch to timer/IRQ ticks.
---

## [2026-02-15 21:55:22 CST] - US-008 (Trace Diff)
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `rust_plc::trace_diff` module to compare SIL `trace.jsonl` and board `trace.jsonl` and produce a structured mismatch report (first mismatch + type + context window).
  - Added CLI `trace-diff` subcommand to generate `report.json` from two trace files.
  - Added unit + CLI integration tests covering both matching traces and a reason-mismatch case.
- Files changed
  - `src/trace_diff.rs`, `src/lib.rs`, `src/main.rs`, `tests/trace_diff.rs`, `prd.json`, `progress.txt`
---

## [2026-02-16 15:12:28 CST] - Roadmap Follow-up (M4/M3 partial closeout)
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `build-rp2040 --emit-uf2 <file>` to continue from `.plc` build artifacts into RP2040 firmware compile + UF2 conversion in one CLI flow, with explicit `--io-map` requirement for wiring contract safety.
  - Added trace regression gate capability via `trace-diff --fail-on-mismatch` (writes report JSON and exits non-zero when mismatch is detected).
  - Extended RP2040 downlink subset to support `set_analog` actions (`AO<n>` resolution in runtime bridge), and extended I/O mapping contracts/templates to include `[analog_outputs]`.
  - Updated RP2040 docs in README with staged flow details, new gating flag, and current downlink support boundaries.
- Files changed
  - `src/main.rs`, `tests/build_rp2040.rs`, `tests/trace_diff.rs`, `src/runtime_bridge.rs`, `tests/runtime_bridge_us006.rs`, `src/io_map.rs`, `crates/board-rp2040/build.rs`, `crates/board-rp2040/src/main.rs`, `README.md`, `progress.txt`
---
