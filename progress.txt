# Ralph Progress Log
Started: Mon Feb 23 13:05:43 CST 2026
---

## Codebase Patterns
- Topology parser now supports mixed entries in `[topology]`; when any explicit `relation { ... }` is present, `TopologySection.connections` should come from relation blocks instead of auto-derived edges.
- Use dedicated parser helpers (`expect_port_reference`, `expect_topology_relation`) for relation fields so errors stay localized to `relation.from` / `relation.to` / `relation.via`.
- Topology relation handling should stay single-source: parser/semantic/topology-gate all consume `TopologySection.connections` only (no attribute-based fallback).

## [2026-02-23 13:29:00 CST] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Extended grammar with `relation` block syntax in `[topology]` (`from: Device.Port`, `to: Device.Port`, `via: driven_by|reports_to|detects`).
  - Added parser support to collect explicit relation declarations into `TopologySection.connections`.
  - Added strict field validation for missing/duplicate `from`/`to`/`via` with actionable parse errors.
  - Added parser tests for explicit relation parse success and required-field failures.
- Files changed
  - src/parser/plc.pest
  - src/parser/mod.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Keep relation-field parsing isolated from attribute parsing to avoid coupling relation DSL rollout with device attribute compatibility changes.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - When changing parser files in this repo, preserve CRLF line endings in `src/parser/mod.rs` to avoid noisy diffs.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing `parses_new_relation_fields_and_ports_into_ast` test still validates legacy attribute-derived edges; keep it green until US-002 removes legacy path.
---
## [2026-02-23 14:41:41 CST] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Removed parser fallback that auto-derived topology edges from `driven_by`/`reports_to`/`detects`; `[topology]` connections now come only from explicit `relation { ... }` blocks.
  - Deprecated legacy topology attributes in device blocks: parser now returns migration errors for `driven_by` / `reports_to` / `detects` and points users to `relation + Device.Port`.
  - Removed semantic and topology gate fallbacks that rebuilt connections from legacy attributes; both now consume only `TopologySection.connections`.
  - Updated parser/topology-gate unit tests to cover strict relation-only flow and migration diagnostics.
- Files changed
  - src/parser/mod.rs
  - src/semantic/mod.rs
  - src/topology_semantic_gate.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Topology relation source is now single-path: parser, semantic graph build, and semantic gate should all read only `TopologySection.connections`.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - Running broad formatting commands can rewrite line endings and create noisy diffs; keep CRLF in core parser/semantic files.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `cargo fmt --check` currently reports pre-existing style drift in `crates/web-server/src/main.rs`; avoid mixing unrelated formatting work into topology stories.
---
## [2026-02-23 16:52:00 CST] - US-003/US-004/US-006/US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - 批量迁移 examples、recovery templates、技能夹具与核心验证测试到 `relation { from/to/via }` 新 DSL，移除旧属性写法。
  - 补齐文档与 API 解析测试中的遗留片段（`docs/已实现/stepper_ab_encoder.md`、`crates/web-server/src/main.rs`）并改为端口级 relation。
  - 修复 `tests/verification_capability.rs` 的全套场景，确保 timing/safety/causality/liveness 在新 DSL 下继续覆盖正反例。
  - 全量回归通过：`cargo test --workspace`。
- Files changed
  - examples/*.plc（含 `two_cylinder.plc`、`assembly_station.plc`、`recovery_templates/*` 等）
  - .codex/skills/plc-gen/fixtures/valid/*.plc
  - tests/build_rp2040.rs
  - tests/examples_integration.rs
  - tests/large_demo_coverage.rs
  - tests/runtime_bridge_us006.rs
  - tests/sim_regress.rs
  - tests/verification_capability.rs
  - docs/已实现/stepper_ab_encoder.md
  - crates/web-server/src/main.rs
  - src/verification/{timing.rs,safety.rs,causality.rs}
  - src/semantic/mod.rs
  - src/scenario_resolve.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - `parse_plc` 相关文档测试会扫描 markdown fenced `plc` 代码块，文档片段必须和当前 DSL 同步。
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - 只跑单个 crate 测试容易漏掉 `web-server` 内嵌 PLC 片段；最终仍需 `cargo test --workspace` 兜底。
  - Useful context (e.g., "the evaluation panel is in component X")
    - `tests/verification_capability.rs` 是四类验证能力的“综合回归哨兵”，DSL 破坏性升级后应优先修复并保持通过。
---
