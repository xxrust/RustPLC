# Ralph Progress Log
Started: 2026-03-23 01:07:00 CST
## Codebase Patterns
- Scope for this Ralph run is WPM-v1 Phase 1-3 only. Do not include classify, rework, recirculation, or continuous geometry.
- Workpiece semantics must close through semantic checks, verification, runtime bridge, runtime-core execution, and example regressions.
- ST backend is explicitly out of scope for this Ralph run.
- `crates/runtime-core` is `no_std`, so workpiece token and lineage storage must stay bounded and allocation-free.
- Keep workpiece lineage as an append-only edge store separate from token records; consumed tokens stay in the token store and are traced through lineage by token id.
- Reuse `tests/workpiece_model_phase23.rs`, `tests/examples_integration.rs`, and `examples/workpiece_*.plc` wherever possible.
- Unsupported workpiece behavior must fail explicitly. Do not silently drop semantics in bridge, runtime, or verification.
- `crates/runtime-core/src/lib.rs` keeps most runtime-core fixtures inline; when `Program`, `AxisMotionCommand`, or pending-axis state gains fields, update those in-file test literals together before trusting the crate test baseline.
- When `runtime_core::Action` or `Program` changes, also update exhaustive action matches and static `Program` fixtures in `src/main.rs`, `src/sim_regress.rs`, `crates/codegen/src/lib.rs`, and `crates/sim/*`; those crates compile against runtime-core directly and fail fast on stale variants/fields.
- Runtime ingress seeding must be driven by actions that actually consume a source endpoint (`acquire` / `transfer` from that endpoint), not by blindly preloading every declared `ingress_site`; otherwise mount-only carrier flows start with falsely occupied slots.
- Phase 2 carrier flows need token metadata that distinguishes “free-standing at slot X” from “mounted on slot X”; `current_location` alone is not enough once `mount` / `transform carrier` / `unmount` are executable.
- Phase 1 workpiece effects require a unique active token at the source endpoint. Treat `0` as underflow, `>1` as duplicate occupancy ambiguity, and destination-capacity violations as explicit overflow errors.
- When semantic already blocks a malformed workpiece contract but verification still needs its own regression, build the normal `ConstraintSet`/`StateMachine` first and then mutate the lowered structures in the test to isolate verification responsibility.
- Carrier-slot bridge lowering must expand `carrier.slot[*]` / grid selectors into concrete runtime ingress-egress endpoints and synthesized carrier `workpiece_sites`; otherwise runtime seeding and slot-addressed actions drift apart even when IR is correct.
- Phase 2 safety verification must mirror runtime token semantics: track `mounted_slot` separately, let `mount` reuse a same-type free-standing token already at the slot, treat `acquire/transfer/finish` as free-standing-only, and keep mounted association intact across `transform carrier`.

## [2026-03-23 01:40:23 +08:00] - US-001
Thread: https://ampcode.com/threads/unknown
- Implemented bounded `WorkpieceTokenStore` in `crates/runtime-core/src/lib.rs` using fixed-size arrays only, with token id, workpiece type, current location, active flag, and terminal status tracking.
- Added token store operations for create, move, finish, lookup, and active occupancy counting, plus a `Runtime` accessor so later execution stories can consume the storage without redesigning the type.
- Added runtime-core tests covering token creation, movement, terminal completion retention, and store capacity overflow; also synced existing runtime-core fixtures to current `Program` and `AxisMotionCommand` fields so the crate test baseline compiles again.
- Files changed
- `crates/runtime-core/src/lib.rs`
- `prd.json`
- `progress.txt`
- **Learnings for future iterations:**
- `crates/runtime-core` unit tests are concentrated in the same source file, so API surface changes in that crate often require fixture updates in dozens of nearby static literals.
- The workpiece token store is currently record-preserving: terminal tokens stay stored and become inactive instead of being deleted, which keeps traceability available for later execution and lineage stories.
---
## [2026-03-23 02:07:00 +08:00] - US-002
Thread: https://ampcode.com/threads/unknown
- Implemented Phase 1 workpiece lowering in `src/runtime_bridge.rs` by removing the global workpiece rejection gate, lowering workpiece type/site/holder metadata into `runtime_core::Program`, and converting `acquire`/`transfer`/`finish` effects into explicit runtime actions.
- Kept unsupported later-phase workpiece behavior explicit: slot-based Phase 2 flows and `mount`/`unmount`/`split`/`merge`/`transform carrier` now fail with `BridgeError::UnsupportedWorkpieceEffect` instead of a coarse model-wide rejection.
- Extended `crates/runtime-core/src/lib.rs` with Phase 1 workpiece metadata structs plus placeholder runtime actions that currently surface explicit unsupported-runtime errors until US-003 implements execution semantics.
- Updated downstream runtime-core consumers (`crates/codegen`, `crates/sim`, `src/main.rs`, `src/sim_regress.rs`) to account for the new `Program` fields and `Action` variants, then added regressions proving the Phase 1 example bridges successfully while Phase 2 effects still fail explicitly.
- Files changed
- `AGENTS.md`
- `crates/board-rp2040/build.rs`
- `crates/codegen/src/lib.rs`
- `crates/runtime-core/src/lib.rs`
- `crates/sim/src/control_kpi.rs`
- `crates/sim/src/lib.rs`
- `crates/sim/src/plant.rs`
- `crates/sim/src/runner.rs`
- `crates/sim/src/scenario.rs`
- `crates/sim/src/waveform.rs`
- `prd.json`
- `progress.txt`
- `src/main.rs`
- `src/runtime_bridge.rs`
- `src/sim_regress.rs`
- `tests/workpiece_model_phase1.rs`
- `tests/workpiece_model_phase23.rs`
- **Learnings for future iterations:**
- `runtime_bridge` must lower `transition.effects` alongside `transition.actions`; otherwise workpiece semantics disappear before runtime even if IR and verification are correct.
- Phase 1 bridge support still depends on a single declared workpiece type because `acquire`/`transfer`/`finish` effects are untyped in IR; multi-type runtime support needs new effect shapes or extra metadata, not bridge-side guessing.
- Adding runtime-core workpiece actions before their executor exists is still useful, but they must fail explicitly in `Runtime::tick` to avoid silently accepting semantics that do nothing at runtime.
---

## [2026-03-23 02:23:14 +08:00] - US-003
Thread: https://ampcode.com/threads/unknown
- Implemented Phase 1 workpiece execution in `crates/runtime-core/src/lib.rs`: `Runtime::new` now seeds ingress endpoints with initial tokens, and `Runtime::tick` executes `WorkpieceAcquire`, `WorkpieceTransfer`, and `WorkpieceFinish` instead of returning `UnsupportedWorkpieceEffect`.
- Added explicit runtime errors for workpiece source underflow, duplicate source occupancy ambiguity, destination overflow, endpoint lookup failures, and token-store invariant/capacity issues so Phase 1 failures are observable instead of silent.
- Added runtime-core regressions for the successful site -> holder -> site -> finish flow plus underflow, duplicate occupancy, and overflow failures; added an integration regression in `tests/workpiece_model_phase1.rs` proving the canonical Phase 1 example now executes end-to-end after bridge lowering.
- Files changed
- `AGENTS.md`
- `crates/runtime-core/src/lib.rs`
- `prd.json`
- `progress.txt`
- `tests/workpiece_model_phase1.rs`
- **Learnings for future iterations:**
  - Phase 1 runtime semantics currently rely on ingress seeding because bridge-lowered `acquire/transfer/finish` effects still do not carry instance ids; if later stories add true instance creation, revisit `Runtime::new` seeding instead of layering ad-hoc exceptions on top.
  - When a workpiece action names only endpoints, source selection must stay deterministic. Runtime should reject multi-token source occupancy explicitly rather than guessing which token to move or finish.
  - A focused integration test in `tests/workpiece_model_phase1.rs` is enough to prove bridge + runtime wiring for the canonical example without waiting for the broader example-gate story.
---

## [2026-03-23 02:38:59 +08:00] - US-004
Thread: https://ampcode.com/threads/unknown
- Reworked `src/verification/safety.rs` workpiece preflight from endpoint-count propagation to reachable token-state propagation for `acquire`/`transfer`/`finish`/`mount`/`unmount`, so safety now validates ingress introduction, terminal bucket routing, unique-source occupancy, and dangling tokens on concrete reachable states.
- Kept the public verification summary shape unchanged while adding exact workpiece diagnostics for undeclared ingress introduction, duplicate occupancy, wrong egress bucket, undefined endpoints, and capacity overflow.
- Extended `tests/workpiece_model_phase23.rs` with negative verification coverage for undeclared ingress, reachable duplicate occupancy, and wrong egress bucket; existing overflow and dangling-token regressions continue to pass against the new model.
- Files changed
- `AGENTS.md`
- `prd.json`
- `progress.txt`
- `src/verification/safety.rs`
- `tests/workpiece_model_phase23.rs`
- **Learnings for future iterations:**
  - Workpiece verification is easier to keep aligned with runtime if reachable-state search tracks active tokens directly instead of only endpoint counters; token-state search can still stay finite by discarding finished tokens from the search state.
  - Some workpiece safety regressions must be verification-only because semantic rejects them earlier; mutate lowered `ConstraintSet` data in tests rather than weakening parser or semantic guards.
  - For Phase 1 ingress checks, seeding should stay tied to reachable `acquire/transfer` source endpoints; applying global ingress seeding to `mount`/`unmount` flows changes existing Phase 2 verification semantics.
---

## [2026-03-23 02:55:50 +08:00] - US-005
Thread: https://ampcode.com/threads/unknown
- Implemented Phase 2 bridge lowering in `src/runtime_bridge.rs` by expanding carrier slot patterns into concrete runtime endpoints, synthesizing carrier slots as runtime `workpiece_sites`, and lowering `mount` / `unmount` / `transform carrier` into explicit runtime actions.
- Kept later-phase behavior explicit: `split` / `merge` still fail at bridge time with effect-level `UnsupportedWorkpieceEffect`, while malformed slot references and undeclared carriers now raise dedicated bridge errors instead of being hidden behind generic unsupported-effect failures.
- Extended `crates/runtime-core/src/lib.rs` and downstream runtime-core consumers so the new Phase 2 action variants compile cleanly everywhere; runtime still rejects them explicitly until US-006 lands.
- Added regression coverage in `tests/workpiece_model_phase23.rs` using `examples/workpiece_carrier_slot_transfer.plc` plus mutated lowered fixtures for unknown-carrier and invalid-slot bridge failures.
- Files changed
- `AGENTS.md`
- `crates/codegen/src/lib.rs`
- `crates/runtime-core/src/lib.rs`
- `prd.json`
- `progress.txt`
- `src/main.rs`
- `src/runtime_bridge.rs`
- `src/sim_regress.rs`
- `tests/workpiece_model_phase23.rs`
- **Learnings for future iterations:**
  - Carrier-slot examples only become runtime-executable after bridge concretizes wildcard ingress/egress patterns into real slot endpoints; leaving wildcard strings in runtime metadata breaks `Runtime::new` token seeding and later slot-addressed actions.
  - If semantic already rejects malformed carrier slot syntax, bridge-specific negative tests should mutate lowered `ConstraintSet` or `StateMachine` data to exercise `UnknownWorkpieceCarrier` and `InvalidWorkpieceSlotReference` directly.
  - When a new workpiece action variant is introduced in `runtime_core::Action`, `src/main.rs`, `src/sim_regress.rs`, and `crates/codegen/src/lib.rs` need matching updates even if the runtime executor still returns `UnsupportedWorkpieceEffect`.
---

## [2026-03-23 03:12:39 +08:00] - US-006
Thread: https://ampcode.com/threads/unknown
- Implemented Phase 2 runtime execution in `crates/runtime-core/src/lib.rs`: `mount`, `unmount`, and `transform carrier` now execute instead of returning `UnsupportedWorkpieceEffect`.
- Extended runtime token state with explicit `mounted_slot` metadata so carrier-slot occupancy can distinguish a free-standing token at a slot from a token mounted on that slot, and kept mounted association intact across `transform carrier`.
- Tightened runtime ingress seeding to endpoints that are actually consumed as action sources (`acquire` / `transfer` sources), which keeps mount-only carrier flows aligned with verification instead of pre-filling every declared ingress slot at `Runtime::new`.
- Added runtime-core regressions for Phase 2 success, empty-slot unmount, duplicate mount, and slot overflow, and added an integration regression in `tests/workpiece_model_phase23.rs` proving the canonical mount/transform/unmount fixture executes end-to-end after bridge lowering.
- Files changed
- `AGENTS.md`
- `crates/runtime-core/src/lib.rs`
- `prd.json`
- `progress.txt`
- `tests/workpiece_model_phase23.rs`
- **Learnings for future iterations:**
  - Carrier slot ingress and carrier mounting are separate semantics. If a future story only stores `current_location`, it will not be able to tell “token arrived at slot” apart from “token is mounted on carrier slot”.
  - Runtime ingress seeding should mirror verification reachability rules: seed only endpoints that a runtime action can consume as a source, otherwise mount-only fixtures create false duplicate occupancy before the first tick.
  - The local `cargo fmt` toolchain currently rejects this repo's existing let-chain syntax even though `cargo test` passes, so formatting cannot be treated as a reliable gate until rustfmt/toolchain alignment is fixed.
---
## [2026-03-23 03:28:35 +08:00] - US-007
Thread: https://ampcode.com/threads/unknown
- Implemented exact Phase 2 carrier reachable-state checks in `src/verification/safety.rs` by extending workpiece flow tokens with explicit `mounted_slot` state and separating free-standing versus mounted source selection.
- Updated safety-side `mount` semantics to reuse an existing same-type free-standing token already at the slot before introducing a new mounted token, which prevents verification from inventing impossible free-standing-plus-mounted duplicates.
- Added regressions in `tests/workpiece_model_phase23.rs` for real slot overflow and for consuming a mounted token after `transform carrier`, proving mounted association survives carrier transforms until `unmount`.
- Files changed
- `AGENTS.md`
- `prd.json`
- `progress.txt`
- `src/verification/safety.rs`
- `tests/workpiece_model_phase23.rs`
- **Learnings for future iterations:**
  - Phase 2 verification has to mirror runtime token shape, not just endpoint occupancy counts; otherwise slot consumers silently collapse mounted and free-standing semantics.
  - A last step without a successor transition never executes its `effect` during reachable-state exploration, so negative verification fixtures that must exercise an effect need a following step or another outgoing edge.
  - Slot overflow coverage is more meaningful when the slot is already occupied and another action tries to place a different token there; duplicate `mount` mostly models duplicate-mounted occupancy rather than generic capacity overflow.
---
## [2026-03-23 03:39:36 +08:00] - US-008
Thread: https://ampcode.com/threads/unknown
- Added bounded `WorkpieceLineageStore` and lineage record types in `crates/runtime-core/src/lib.rs` to model `split parent -> child` and `merge input -> output` relations without allocation.
- Wired lineage storage into `Runtime` as a separate append-only structure next to `WorkpieceTokenStore`, while keeping execution semantics unchanged until the later `split` and `merge` stories land.
- Added runtime-core tests covering split lineage lookup, merge lineage lookup, consumed-token traceability, and lineage capacity overflow; also preserved the pattern in `AGENTS.md` for future runtime workpiece stories.
- Files changed
- `AGENTS.md`
- `crates/runtime-core/src/lib.rs`
- `prd.json`
- `progress.txt`
- **Learnings for future iterations:**
  - Treat lineage as edges between token ids, not extra mutable state on the token itself; that keeps split one-to-many and merge many-to-one representable without changing token identity rules.
  - Consumed workpiece traceability depends on two stores working together: token records remain in `WorkpieceTokenStore` with `terminal_status = Consumed`, while lineage keeps the relationship history in an append-only log.
  - Adding a future `split` or `merge` executor should populate lineage through dedicated store helpers instead of inventing ad-hoc per-effect side tables.
---
