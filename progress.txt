# Ralph Progress Log
## Codebase Patterns
- Online variable control in `sim-plc` is semantic (not audit-only) only when commands resolve to DI/AI channels; use `--online-var-bindings` for domain names, auto-mapping is limited to `BOOL:DI<n>/REAL:AI<n>`.
- When the repo root is both `[package]` and `[workspace]`, use `cargo test --workspace` to run member-crate tests (plain `cargo test` only targets the default package).
- For `#![no_std]` crates that still want unit tests/doctests, add `#[cfg(test)] extern crate std;`.
- In `src/parser/plc.pest`, when an alternation allows both `measured_value` (e.g. `50bar`) and `number`, list `measured_value` first so `50bar` doesn't partially match as `50`.
- In `src/parser/plc.pest`, when `number` and `integer` are both allowed, list `number` before `integer` to avoid `2.0` being partially consumed as `2`.
- In PLC topology `analog_input { range: A..B }`, the DSL range syntax does not support negative bounds; if you need signed values, model them as offset-encoded (or split sign into a separate discrete signal) and keep `range` non-negative.
- In `if: <cond> goto ... else: goto ...`, `goto` targets must be a task name or `task.step` (step-only names are treated as task names); when branching within the same task, use `goto cycle.<step_name>`.
- Safety rules that relate an unconstrained `external: true` analog input (e.g. `zone_code`) to an actuator state can be unverifiable/unsatisfiable unless the actuator state is unreachable or you model assumptions about the external signal's behavior.
- For multi-coordinate axis setups (count/theta/pos_mm), pick exactly one “primary truth” coordinate; keep conversion + consistency checks in the driver layer and expose discrete health signals (`range_valid`, `pos_consistent`, optional `health_code`) to the DSL.
- In `runtime-core`, `delay`/`timeout` are evaluated as `elapsed = now_tick - step_entered_tick`, and they fire when `elapsed >= configured_ticks` (so `after_ticks: 0` is immediate on the entry tick).
- In `crates/sim::SimIo`, input changes scheduled for tick `N` are applied when `advance_tick()` moves to `N`, so they are visible during the runtime's tick-`N` evaluation (tick-0 changes are applied in `SimIo::new`).
- For reusable `.plc` templates meant to run via `runtime_bridge`, include explicit physical IO devices (`X<N>`, `Y<N>`) plus logical devices connected via `connected_to`, so IO IDs resolve deterministically.
- In `TopologyGraph`, `device A { connected_to: B }` creates an edge `B -> A` (upstream to downstream). For scenario/IO mapping helpers, start from physical IO nodes (`X<N>`, `AI<N>`) and follow outgoing neighbors to discover logical aliases.
- Scenario YAMLs generated by `scenario-init` include a `# Source PLC: <name>` header; use it (or validate it) to catch PLC/scenario mismatches early.
- For per-example scenario suites, store them under `scenarios/<example_stem>/` and regression-test them by running `scenario-validate` + `sim-plc` and asserting presence/absence of `"reason":"timeout"` events in the produced trace.
- If you want scenario YAMLs to reference logical analog input aliases by name, ensure the PLC topology models `connected_to` chains from physical `AI<N>` nodes to logical `analog_input` devices (and semantic connection typing allows analog<->analog logical connections).
- For CLI-driven docs, add a doc consistency test that checks required commands are present both in the markdown playbook and in `rust_plc` usage output.
- For docs with fenced ` ```plc` snippets, keep each snippet a complete PLC file (`[topology]` + `[constraints]` + `[tasks]` in order) and add an integration test that extracts and parses those blocks via `parse_plc` to prevent example drift.
- For repo-local Wiki drafts under `docs/wiki/`, include at least one complete ` ```plc` snippet and gate it with a small test (extract + `parse_plc`) so drafts don't silently drift into non-parseable examples.
- `build-rp2040` validates `io_map.toml` via `IoMap::validate_for_usage()`: if you add a new pin-owning subsystem (e.g. motion), include its pins in the duplicate-GPIO validation there so conflicts fail fast.
- For scope-sensitive deliverables, add an explicit “in-scope vs out-of-scope” section in the canonical doc and lock key boundary phrases with a doc regression test to avoid implicit capability creep.
- For doc deliverables with structural acceptance criteria (required headings/links), add a lightweight regression test that asserts those key strings exist to prevent drift.
- For JSONL contracts that are diff-sensitive, serialize typed structs (not maps) and keep canonical field declaration order so snapshots stay stable.
- For percentile-based gates/reports, use one deterministic nearest-rank rule and lock it with unit tests so threshold behavior stays stable across refactors.
- For `crates/sim` plant-style dynamics, keep the plant as a deterministic tick-step that *returns* input updates, and call it from `SimIo::advance_tick()` (after applying scenario inputs) to avoid borrow-checker pitfalls with `&mut self` callbacks.
- For scenario-side burst generators, enforce tick alignment on `at_ms/period_ms/active_ms` at parse time so replay behavior stays deterministic and seed-independent.
- For discrete dead-time models, use a fixed-length FIFO with `delay_ticks = ceil(dead_time_ms / tick_ms)` so response timing stays deterministic across runs.
- Waveform exports are deterministic by construction: VCD assigns idcodes in a fixed order (`di<N>` then `do<N>`) and uses timestamps `tick * tick_ms` with `$timescale 1ms`.
- For human-friendly YAML parse errors, use `serde_path_to_error` so errors include a field path (e.g., `inputs[0].set.digital_inputs`).
- `serde_yaml` enum representations can be surprising (it expects YAML tags like `!Variant`); for author-friendly scenario files prefer struct-shaped keys like `faults: - sensor_stuck: { at_ms, target, value }`.
- To unit-test verifier behavior that semantic validation would normally reject (e.g., "skipped rule binding"), build constraints normally and then mutate `ConstraintSet` inside the test.
- `src/runtime_bridge.rs` builds `runtime-core::Program<'static>` by leaking boxed slices/strings (`Box::leak`) to keep the runtime API simple for now; treat this as OK for CLI/tests but avoid in long-lived processes.
- The repo is not currently rustfmt-clean; treat `cargo fmt --check` as non-gating unless you are intentionally doing a formatting pass.
- CLI integration tests in this repo should invoke the compiled binary via `env!("CARGO_BIN_EXE_rust_plc")` and verify artifacts on disk.
- The main `<file.plc>` CLI now writes a structured verification report to `out/<stem>.verification_report.json` by default; in tests prefer `--report <temp-file>` to avoid shared output collisions.
- Runtime budget warnings should update `runtime_budget` data before report serialization, so JSON evidence and stderr/deny-warnings behavior stay consistent.
- Batch SIL regress runs live in `rust_plc::sim_regress`: it scans `.plc`/scenario YAMLs recursively, writes per-case `trace.jsonl`/`report.json` under `out/sim-regress/case_XXXX`, and emits a summary JSON.
- No-board trace regression gate is available as `rust_plc no-board-gate`: it runs SIL + `virtual-board` + `trace-diff` and writes `sil_trace.jsonl`, `board.log`, `board_trace.jsonl`, `diff_report.json` under `--out-dir`.
- `no-board-gate` can reuse `tick_timing.jsonl` to enforce realtime thresholds (`--max-p99-exec-us`, `--max-overrun-count`) while keeping trace-diff gating unchanged when thresholds are unset.
- For embedded workspace crates, keep a host-stub `main()` behind `cfg(not(target_os = "none"))` so `cargo test --workspace` stays green while firmware code is `cfg(target_os = "none")`.
- In embedded crates with host-stub builds, gate firmware-only helpers with `#[cfg(any(test, target_os = "none"))]` so `cargo check --workspace` stays warning-clean on host.
- Release-style artifacts should include a `manifest.json` that hashes the exact on-disk files (plus `size_bytes`) so integration tests can verify integrity deterministically.
- When release-bundle adds new evidence artifacts, add them to both manifest hashing and integration-test required lists in the same change to keep delivery contracts auditable.
- For example safety-modeling fixtures, assert `verification.safety.rule_statuses` contains one entry per safety rule and keep `skipped_rules == 0` to catch silently unbound constraints.
- For board text log parsing, use `line.find("TRACE ")` / `line.find("TIMING ")` to tolerate prefixed logs, parse unordered `k=v` tokens, ignore unknown fields, and report missing/invalid required fields with source line numbers.
- Prefer `rust_plc::board_log::parse_board_log_text` as the single entrypoint for mixed `board.log` parsing (TRACE + TIMING), and keep record-specific parsers in `board_trace` / `tick_timing`.
- For RP2040 daily HIL gating, keep multi-case bundles in `scenarios/rp2040_hil_gate/cases.json` and attach per-case assertion specs (`axis/signal/step/tick`) so failures are locatable from `assertions_report.json`.
- For RP2040 abnormal-exit safety audits, keep the A/B/C/D source matrix in `scenarios/rp2040_hil_gate/abnormal_exit/matrix.json`, per-class evidence in `.../evidence/<class>.json`, and verify with `scripts/abnormal_exit_matrix_verify.py` (default auto-required classes are A/B/C; D remains `hardware_only` manual-chain validation).
- For command-line diagnostics in CI, prefer `--output json` on `scenario-validate`, `scenario-doctor`, `no-board-gate`, and `build-rp2040`, and consume stable issue codes (`SCN-*`, `GATE-*`, `BLD-*`) instead of brittle free-text matching.
- Cross-platform CI for shell tooling should run `scripts/ci_script_contract_preflight.sh` early, enforce git mode `100755` on required gate scripts, and reject CRLF in tracked `*.sh`.
- For board HIL timing SLAs, generate a standalone `timing_gate_verdict.json` artifact (thresholds + observed metrics + violations) and make summary artifacts reference it directly.
- For `hardware_only` Class-D evidence, require per-attachment `provenance.source_path` and keep optional digest/operator/time metadata for audit traceability.

Started: Thu Feb 19 14:03:44  2026
## [2026-02-19 14:08:29 CST] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID (not available in this Codex CLI environment)
- What was implemented
  - Added semantic runtime binding for online variable control in `sim-plc` (BOOL/REAL):
    - new CLI flag: `--online-var-bindings <bindings.toml>`
    - supported binding schema (`schema_version=1`, `[bool]`, `[real]`)
    - variable commands now inject deterministic force events into runtime-evaluated DI/AI channels.
  - Added fast-fail validation for missing/invalid bindings with actionable error hints.
  - Preserved deterministic replay semantics (tick alignment + sorted command order) and extended audit rows with resolved `bound_channel`.
  - Updated `sim-plc` usage/help text to include the new bindings argument.
  - Updated docs/wiki to describe binding format, runtime effect, and safety boundary.
  - Updated integration tests:
    - verify runtime semantic effect (trace contains action transitions due to variable binding)
    - verify deterministic replay with bindings
    - verify missing-binding failure path
    - keep tick-misalignment guard coverage.
  - Initialized Stage-4 planning context:
    - archived prior stage `prd.json`/`progress.txt` under `archive/2026-02-19-ralph-stage3-operability-hardening/`
    - created new Stage-4 `prd.json` and refreshed `progress.txt` header.
- Quality checks
  - `cargo test --test online_variable_control_plane`
  - `scripts/stage3_runtime_dev_gate.sh`
  - `cargo check --workspace`
- Files changed
  - `src/main.rs`
  - `tests/online_variable_control_plane.rs`
  - `docs/runtime_online_variable_control.md`
  - `docs/wiki/Runtime-Online-Variable-Control-Plane.md`
  - `prd.json`
  - `progress.txt`
  - `archive/2026-02-19-ralph-stage3-operability-hardening/prd.json`
  - `archive/2026-02-19-ralph-stage3-operability-hardening/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For dev control planes, semantic efficacy should be explicit: script parsing + runtime injection + audit must be delivered together.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - If online-variable names are domain-friendly (e.g., `diag_latch`), you need explicit bindings; auto mapping is only safe for channel-like names (`DI<n>/AI<n>`).
  - Useful context (e.g., "the evaluation panel is in component X")
    - `bound_channel` in `online_var_audit.jsonl` is now the direct bridge between variable-level commands and runtime IO-level effects.
---
## [2026-02-19 14:18:42 CST] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID (not available in this Codex CLI environment)
- What was implemented
  - Added repository-level script line-ending policy in `.gitattributes` (`*.sh`/`*.py` => LF) to stop platform drift.
  - Added `scripts/ci_script_contract_preflight.sh` to enforce required gate-script executable metadata (`100755`) and fail on CRLF in tracked shell scripts.
  - Hooked preflight into both CI (`.github/workflows/rp2040_regression.yml`) and local umbrella gate (`scripts/stage3_runtime_dev_gate.sh`) so failures surface early.
  - Updated Stage-3 CI runbook with preflight execution and remediation guidance.
  - Added `tests/stage3_ci_gate_runbook_doc.rs` to lock required runbook preflight content.
- Quality checks
  - `scripts/ci_script_contract_preflight.sh`
  - `cargo test --test stage3_ci_gate_runbook_doc`
  - `scripts/stage3_runtime_dev_gate.sh`
  - `cargo check --workspace`
- Files changed
  - `.gitattributes`
  - `.github/workflows/rp2040_regression.yml`
  - `scripts/ci_script_contract_preflight.sh`
  - `scripts/rp2040_hil_daily_gate.sh` (mode fix to executable)
  - `scripts/stage3_runtime_dev_gate.sh`
  - `docs/stage3_ci_gate_runbook.md`
  - `tests/stage3_ci_gate_runbook_doc.rs`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For CI script reliability, enforce mode and EOL contracts via a dedicated preflight script and run it before longer test suites.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - `git ls-files --stage` reflects tracked metadata only; new scripts must be `git add --chmod=+x` to ensure CI sees executable mode.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `scripts/stage3_runtime_dev_gate.sh` is the local developer parity entrypoint for `stage3-runtime-dev-gates` workflow behavior.
---
## [2026-02-19 14:24:08 CST] - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID (not available in this Codex CLI environment)
- What was implemented
  - Added `scripts/timing_gate_verdict.py` to evaluate `timing_report.json` against optional thresholds and emit a stable verdict artifact (`timing_gate_verdict.json`).
  - Updated `scripts/rp2040_trace_gate.sh` to always generate timing verdicts, print concise timing gate status context, and fail deterministically when thresholds are violated.
  - Updated `scripts/rp2040_hil_gate.sh` and `scripts/rp2040_hil_daily_gate.sh` summaries to include timing artifacts (`timing_report.json` + `timing_gate_verdict.json`) as first-class outputs.
  - Expanded docs with threshold usage and tuning workflow:
    - `docs/hil_regression.md`
    - `docs/board_rp2040.md`
  - Added regression tests:
    - `tests/timing_gate_verdict_script.rs` (threshold pass/fail contract)
    - `tests/hil_regression_doc.rs` (doc contract for timing gate workflow)
- Quality checks
  - `cargo test --test timing_gate_verdict_script --test hil_regression_doc`
  - `cargo check --workspace`
- Files changed
  - `scripts/timing_gate_verdict.py`
  - `scripts/rp2040_trace_gate.sh`
  - `scripts/rp2040_hil_gate.sh`
  - `scripts/rp2040_hil_daily_gate.sh`
  - `docs/hil_regression.md`
  - `docs/board_rp2040.md`
  - `tests/timing_gate_verdict_script.rs`
  - `tests/hil_regression_doc.rs`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - HIL threshold outcomes are easier to audit when pass/fail logic emits its own machine-readable verdict artifact instead of only stderr text.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - If threshold checks live in shell wrappers, wire verdict file paths through all summary levels (`trace_gate` -> `hil_summary` -> `daily_case_summary`) or operators lose visibility.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `timing_gate_verdict.json.violations` is now the canonical location for realtime threshold breach details.
---
## [2026-02-19 14:27:23 CST] - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID (not available in this Codex CLI environment)
- What was implemented
  - Extended Class-D evidence schema so every manual attachment carries provenance metadata (`provenance.source_path` required; digest/operator/time optional).
  - Updated Class-D template + live D evidence fixture to include provenance payloads.
  - Hardened verifier (`scripts/abnormal_exit_matrix_verify.py`) to validate provenance structure/required fields and report precise attachment-path errors.
  - Updated abnormal-exit docs/wiki to explicitly describe provenance requirements and link the workflow docs.
  - Expanded regression coverage:
    - fixture-level checks for provenance presence in D evidence/template
    - verifier failure test for missing attachment provenance
    - doc contract test now locks provenance wording.
- Quality checks
  - `cargo test --test abnormal_exit_matrix --test abnormal_exit_matrix_doc`
  - `cargo check --workspace`
- Files changed
  - `scenarios/rp2040_hil_gate/abnormal_exit/evidence_schema.json`
  - `scenarios/rp2040_hil_gate/abnormal_exit/class_d_checklist_template.json`
  - `scenarios/rp2040_hil_gate/abnormal_exit/evidence/D.json`
  - `scripts/abnormal_exit_matrix_verify.py`
  - `docs/abnormal_exit_matrix.md`
  - `docs/wiki/Class-D-Abnormal-Exit-Evidence-Workflow.md`
  - `tests/abnormal_exit_matrix.rs`
  - `tests/abnormal_exit_matrix_doc.rs`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Manual safety evidence becomes reviewable at scale only when attachments include provenance fields that point back to original captures.
  - Gotchas encountered (e.g., "do not forget to update Z when changing W")
    - If Class-D evidence shape changes, update schema + template + D fixture + verifier + tests together to avoid drift.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Verifier errors now include precise attachment field paths (`class_d_manual.attachments[i].provenance.*`) for fast audit remediation.
---
