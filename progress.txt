# Ralph Progress Log
Started: 2026-03-09 00:00:00 CST

## Codebase Patterns
- `axis.move_relative/absolute` 参数解析走四层：`src/parser/plc.pest` → `src/parser/mod.rs` → `src/ast/mod.rs` → `src/semantic/mod.rs`，改动必须同步。
- 轴动作参数的语义优先级固定为 `inline overrides > action params 引用 > device.motion_param_set`，并在语义阶段写回最终值再降级到 IR。
- 轴故障语义需跨层同构：`src/ir/mod.rs` 与 `crates/runtime-core/src/lib.rs` 的 `AxisFaultKind/AxisFaultCategory` 必须同名同语义，并通过语义降级统一填充 `category/vendor_code`。
- `pest` 规则存在 `boolean_value` 与 `identifier` 二选一时，必须把 `boolean_value` 放在前面，否则 `true/false` 会被吞成标识符并触发类型误报。
- `parallel` 合成状态命名约定固定：`__parallel_<n>_fork` → `__parallel_<n>_branch_<m>_active` → `__parallel_<n>_branch_<m>_done` → `__parallel_<n>_join`，回归测试与验证逻辑依赖该模式。
- `runtime_core::Program` 结构字段变更需同步四处：`src/runtime_bridge.rs`（桥接填充）、`src/main.rs`（内置 `SIM_PROGRAM`）、`crates/codegen/src/lib.rs`（生成器模板）、`crates/board-rp2040/build.rs`（默认固件程序模板）。
- 轴故障停机可观测性采用双日志阶段：先 `axis_stop_transition_enter` 再 `axis_stop_transition_completed`，并用 `axis_stop_transition_log_message_id(stop_mode, phase)` 做稳定断言，避免硬编码 message_id。

---

## [2026-03-09 15:04:33 CST] - US-001
- What was implemented
  - Implemented 5-layer axis hierarchy in compiler resources and validation flow:
    - `motor_class -> family -> model -> config -> motion_param_set`
  - Extended DSL topology to accept `motion_param_set` on axis devices.
  - Added strict chain validation:
    - `model.family_id` -> `family.motor_class_id`
    - `config.model_id` must match device `model_ref`
    - optional `motion_param_set` compatibility/range checks
  - Added parser + axis profile regression tests for mismatch/out-of-range failures.
- Commit
  - `4f55ad1 feat: [US-001] - 轴参数层级模型完整化（class -> family -> model -> config -> motion_param_set）`
---

## [2026-03-09 15:56:01 CST] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added `axis.move_*` DSL support for `params` reference plus inline `speed/acc/dec` overrides.
  - Enforced semantic resolution priority: inline overrides > action `params` > device `motion_param_set`.
  - Enforced no-default contract for motion actions: missing `speed/acc/dec` now fails with `AXIS-007`.
  - Kept model/config limit enforcement by checking effective `acc/dec` in semantic (`AXIS-009`) and speed upper bound in runtime bridge.
  - Updated axis examples and regression tests across parser/semantic/verification/runtime bridge flows.
- Files changed
  - `AGENTS.md`
  - `src/parser/plc.pest`
  - `src/parser/mod.rs`
  - `src/ast/mod.rs`
  - `src/semantic/mod.rs`
  - `src/verification/safety.rs`
  - `src/verification/causality.rs`
  - `tests/runtime_bridge_us006.rs`
  - `examples/axis_stepper_fault_routing.plc`
  - `examples/axis_servo_fault_routing.plc`
  - `prd.json`
- **Learnings for future iterations:**
  - Patterns discovered: axis action参数解析必须在语义阶段先“收敛”为完整参数，再做 IR 降级，避免桥接层丢失上下文。
  - Gotchas encountered: `pest` 的 wrapper 规则（如 `axis_move_relative_arg`）若不先解包，会导致字段丢失并触发误报“缺少参数”。
  - Useful context: 旧有 `bridge_rejects_axis_move_speed_exceeding_profile_limit` 回归依赖“速度上限由 bridge 报错”，语义层不应抢先拦截该路径。
---

## [2026-03-09 16:28:00 CST] - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Introduced unified axis fault model in **runtime-core**: `AxisFaultCategory` (`Recoverable/NonRecoverable/Safety`) + `AxisFaultKind` (`Reject/Motion/Safety/Vendor`) + `AxisFault` payload.
  - Migrated runtime axis callback contract from split variants (`Reject/MotionFault/SafetyFault`) to `AxisMotionResult::Fault(AxisFault)` and unified surfaced runtime error as `RuntimeError::AxisFault`.
  - Introduced matching axis fault taxonomy in **IR** (`src/ir/mod.rs`) and made semantic lowering (`lower_axis_fault_branch`) derive `category/vendor_code` from `kind` to keep cross-layer consistency.
  - Updated codegen/IR/runtime bridge tests and added vendor-slot regression assertions for both IR and runtime-core.
- Files changed
  - `AGENTS.md`
  - `crates/runtime-core/src/lib.rs`
  - `src/ir/mod.rs`
  - `src/semantic/mod.rs`
  - `src/codegen/st.rs`
  - `tests/runtime_bridge_us006.rs`
  - `prd.json`
- **Learnings for future iterations:**
  - Patterns discovered: 轴故障语义字段一旦升级，最容易漏掉的是 runtime-core 测试和 bridge 集成测试的断言类型，建议优先同步这两处。
  - Gotchas encountered: 若 `AxisFaultBranch` 只改 `kind` 不同步 `category/vendor_code`，会造成 IR 与 runtime 语义漂移；应在语义层集中推导。
  - Useful context: `AxisMotionResult::reject/motion_fault/safety_fault` 构造器能减少测试样板并稳定分类语义。
---

## [2026-03-09 17:02:17 CST] - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added new topology declaration `axis_fault_contract <name> { ... }` with parser grammar, AST schema, and IR lowering.
  - Introduced strict required-field parsing (no implicit defaults) for `axis/severity/stop_mode/auto_reset_policy/manual_ack_required` with duplicate-field diagnostics.
  - Added semantic validation to ensure contract axis targets an existing motion axis device (`stepper_motor`/`servo_drive`) and prevents duplicate contract bindings per axis.
  - Added parser/semantic/IR roundtrip regression tests for both success and failure paths.
- Files changed
  - `AGENTS.md`
  - `src/ast/mod.rs`
  - `src/parser/plc.pest`
  - `src/parser/mod.rs`
  - `src/ir/mod.rs`
  - `src/semantic/mod.rs`
- **Learnings for future iterations:**
  - Patterns discovered: 拓扑级新声明（如 `axis_fault_contract`）应沿 `plc.pest -> parser -> ast -> semantic -> ir` 一次性贯通，否则会出现“解析成功但降级丢失”的层间断裂。
  - Gotchas encountered: `manual_ack_required: true/false` 这类字段若 grammar 中 `identifier` 排在 `boolean_value` 前，会被错误识别为标识符并产生误导性类型报错。
  - Useful context: 当前语义层已将“axis 合同仅可绑定轴设备”前置校验，后续 US-006 可直接在 IR `TopologyGraph.axis_fault_contracts` 上扩展策略矩阵执行。
---

## [2026-03-09 18:59:54 CST] - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - 将 `parallel` 合成分支状态统一命名为 `branch_<n>_active`，并保留 `branch_<n>_done`，使 fork/join 状态图语义更清晰且可稳定回归。
  - 新增 US-005 集成回归：覆盖并行同 tick 双分支启动、race 同时满足时首分支确定性胜出、以及 A 先完成/B 后完成的 join 行为。
  - 修复一个阻塞测试的现有问题：`src/codegen/st.rs` 中并行/竞速合成状态单测的 `State` move 后复用导致编译失败（改为 clone）。
- Files changed
  - `AGENTS.md`
  - `src/semantic/mod.rs`
  - `src/codegen/st.rs`
  - `tests/parallel_race_us005.rs`
  - `prd.json`
- **Learnings for future iterations:**
  - Patterns discovered: 并行语义相关改动要优先做 runtime 集成回归（trace/step 断言），仅看语义层状态图容易漏掉桥接行为偏差。
  - Gotchas encountered: Rust 单测里把 `State` move 进 `Vec` 后再复用会触发 E0382，类似构造器样例建议统一 `clone()`。
  - Useful context: 现有 bridge 已支持 `condition+always` 形状（按 `after_ticks=0` 处理），可用于 race 分支的确定性优先级判定测试。
---

## [2026-03-09 19:22:51 CST] - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - 将 `axis_fault_contract` 策略矩阵降级到 runtime：`src/runtime_bridge.rs` 现在会把 IR 合同映射为 `runtime_core::Program.axis_fault_policies`。
  - 在 `runtime-core` 新增策略模型（severity/stop_mode/auto_reset/manual_ack）与日志编码函数；轴动作返回 fault 时按策略产生日志事件 `axis_fault_policy_applied`。
  - 新增 `Runtime::tick_with_axis_and_logs` 以便在运行轴动作时采集策略日志，便于回归验证与审计。
  - 增加 US-006 回归测试，验证 bridge 对策略矩阵字段的保真映射，以及 runtime 故障路径上的策略日志编码一致性。
- Files changed
  - `AGENTS.md`
  - `crates/runtime-core/src/lib.rs`
  - `src/runtime_bridge.rs`
  - `tests/runtime_bridge_us006.rs`
  - `crates/codegen/src/lib.rs`
  - `crates/board-rp2040/build.rs`
  - `src/main.rs`
  - `prd.json`
- **Learnings for future iterations:**
  - Patterns discovered: 轴故障策略矩阵应在 bridge 阶段一次性降级到 runtime Program，运行时仅做执行与日志，不重复解析 DSL/IR 结构。
  - Gotchas encountered: 给 `runtime_core::Program` 增字段时，除桥接外还需同步更新代码生成模板和默认固件 Program 字符串，否则会在下游 crate 出现结构体字面量编译错误。
  - Useful context: `axis_fault_policy_log_message_id(...)` 是策略日志断言的稳定入口，集成测试可直接以该函数构造期望值避免硬编码位编码细节。
---

## [2026-03-09 19:44:04 CST] - US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - 在 `runtime-core` 引入轴停机状态机：`AxisStopState`，并提供 `Runtime::axis_stop_state()` 用于观测停机结果。
  - 将策略矩阵中的 `stop_mode` 落地为确定性停机迁移：`Running -> {ControlledStopping|QuickStopping|ImmediateStopping} -> Stopped`。
  - 新增停机迁移审计日志：`axis_stop_transition_enter` / `axis_stop_transition_completed`，并通过 `axis_stop_transition_log_message_id(stop_mode, phase)` 编码。
  - 扩展回归测试覆盖 controlled/quick/immediate 三条停机路径，验证 AxisFault 分类、策略日志与停机迁移日志一致。
- Files changed
  - `AGENTS.md`
  - `crates/runtime-core/src/lib.rs`
  - `tests/runtime_bridge_us006.rs`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: 停机语义建议统一通过“策略日志 + 停机迁移日志”双轨审计，既保留策略矩阵上下文，也保留执行阶段信息。
  - Gotchas encountered: `runtime_core::Program` 结构体扩展后，`crates/runtime-core/src/lib.rs` 单测中所有 `Program` 字面量都需要同步字段，否则 `cargo test -p runtime-core` 会批量报 `E0063`。
  - Useful context: `Runtime::tick_with_axis_and_logs` 已可一次性采集策略日志和停机迁移日志，适合作为 axis fault 行为回归的统一入口。
---
