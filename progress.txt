# Ralph Progress Log
Started: Thu Feb 26 2026
---

## Codebase Patterns
- Cam 插值语义在 `src/semantic/mod.rs` 生成系数，运行时只消费系数；样条问题优先在语义层修复并配套连续性测试。
- 安全阈值建模走 `verification/safety.rs` 的离散区间逻辑；新增模拟量端口阈值时要同步补齐 `compute_analog_regions` 与端口域注册。
- Cam 相关回归应覆盖 parser/semantic/runtime/runtime_bridge/verification 五层，使用 `scripts/cam_regression_gate.sh` 统一入口。

## [2026-02-26 13:37:56 CST] - US-001
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 实现 periodic 三次样条（循环方程求解），不再退化为线性系数。
  - 新增 periodic 边界 C2 连续性测试与 `cubic_derivative` 有限差分一致性测试。
- Files changed
  - src/semantic/mod.rs
  - crates/runtime-core/src/lib.rs
- **Learnings for future iterations:**
  - 周期样条在本项目可用“去重尾点 + 循环三对角方程”求解，失败时回退线性可保持稳定。
  - Cam 连续性最好同时测位置/一阶导/二阶导，避免只测值连续导致隐性加速度断点。
---

## [2026-02-26 13:37:56 CST] - US-002
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 新增 `binary_search_interval` 边界/命中/区间点测试。
  - 新增线性插值中值精度测试；保留并验证 periodic wrap 与 oneshot clamp 行为。
- Files changed
  - crates/runtime-core/src/lib.rs
- **Learnings for future iterations:**
  - 区间二分的“精确命中中点归右段”是当前约定，测试里要显式锁定。
---

## [2026-02-26 13:37:56 CST] - US-003
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 增加 `Runtime::new` 对 `TooManyCamCouplings` 与初始 `table_index` 越界的测试。
  - 增加 `cam_phase` 越界动作错误测试，并保持既有越界语义兼容。
- Files changed
  - crates/runtime-core/src/lib.rs
- **Learnings for future iterations:**
  - `Runtime::new` 的初始化防线比 tick 时更早，越界问题应优先在 init 测试锁死。
---

## [2026-02-26 13:37:56 CST] - US-004
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 新增 `cam_switch` 在 `gear_ratio != 1` 且 `phase_offset != 0` 下的连续性测试。
  - 覆盖 `switch_offset` 与 `switch_decay_ticks` 衰减路径断言。
- Files changed
  - crates/runtime-core/src/lib.rs
- **Learnings for future iterations:**
  - 由于同 tick 可执行多步 Action，测试切表连续性要先插入至少一个延时/阻塞步再切表。
---

## [2026-02-26 13:37:56 CST] - US-005
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 语义层支持 `safety: cam_xy.following_error > N` 这类 `device.port` 阈值目标。
  - 安全验证层补齐端口阈值的离散域构建与端口域映射，并新增正反测试。
- Files changed
  - src/semantic/mod.rs
  - src/verification/safety.rs
- **Learnings for future iterations:**
  - 阈值目标若是端口，需同时处理“目标解析 + 端口是否模拟量 + 区间域构建”三件事，否则会变成 skipped/空阈值细节。
---

## [2026-02-26 13:37:56 CST] - US-006
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 新增 `encoder -> cam -> servo` 因果链正例。
  - 新增主轴断链反例，锁定 `encoder_main -> cam_xy` 断链报错。
  - 用例覆盖 `cam_engage/cam_switch/cam_phase` 与 wait 组合路径。
- Files changed
  - src/verification/causality.rs
- **Learnings for future iterations:**
  - 因果 tests 里 relation 方向与设备类型约束较严格，`detects/reports_to` 需与 producer/consumer 角色匹配。
---

## [2026-02-26 13:37:56 CST] - US-007
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 增加设备库注入测试，验证 `cam_coupling.toml` 的 `fault.on conflicts_with engage.on` 被注入。
  - 新增 `examples/flying_shear.plc` 并纳入 examples integration 回归。
  - 新增 `examples/error_cam_missing_table.plc` 并验证 undefined_reference 报错。
- Files changed
  - src/semantic/mod.rs
  - tests/examples_integration.rs
  - examples/flying_shear.plc
  - examples/error_cam_missing_table.plc
- **Learnings for future iterations:**
  - examples 进入回归前要先过 liveness（wait 需 timeout 或 allow_indefinite_wait）。
---

## [2026-02-26 13:37:56 CST] - US-008
Thread: https://ampcode.com/threads/N-A-codex-cli
- What was implemented
  - 新增 `scripts/cam_regression_gate.sh`，串行覆盖 parser/semantic/runtime/runtime_bridge/verification 五层。
  - 在 CI `rp2040_regression.yml` 加入 Cam gate 步骤。
- Files changed
  - scripts/cam_regression_gate.sh
  - .github/workflows/rp2040_regression.yml
- **Learnings for future iterations:**
  - 对 `rust_plc` 单元 gate 建议带 `--lib`，可避免跑大量无关 integration 测试并显著提速。
---
