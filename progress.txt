# Ralph Progress Log
Started: Fri Feb 27 23:19:22 CST 2026
## Codebase Patterns
- Run `cargo check --workspace` for typecheck and `cargo test --workspace` for regression gate before committing.
- Keep DSL syntax decisions in dedicated docs under `docs/` and link story completion back to `prd.json` + `progress.txt`.
- For new `[topology]` DSL entries, keep grammar (`src/parser/plc.pest`), parser wiring (`src/parser/mod.rs`), and semantic topology cloning (`src/semantic/mod.rs`) in sync.
- When adding a new `action:` DSL command, keep `action_command` grammar alternatives, `parse_action_statement` match arms, and parser unit tests synchronized.
- Introducing a new IR `TransitionAction` variant requires coordinated updates in semantic lowering, runtime bridge conversion, ST codegen rendering/inference, and verification matchers to keep compile/runtime behavior consistent.
- Extern call semantics rely on predeclared `[topology] variable` symbols for return bindings; signature checks should validate arg/binding arity+types and report step-level line numbers.

---
## [2026-02-28 00:01:57] - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added parser grammar and AST support for `extern function <name>(...) -> ... { ... }` declarations in `[topology]`.
  - Implemented required contract field parsing/validation (`rust_module`, `pure`, `time_bound_us`) with line-aware parse errors for missing fields.
  - Added parser tests covering successful extern declaration parsing and missing-required-field rejection.
  - Marked US-002 as completed (`passes: true`) in `prd.json`.
- Files changed
  - src/ast/mod.rs
  - src/parser/plc.pest
  - src/parser/mod.rs
  - src/semantic/mod.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - `TopologySection` data added by parser should be propagated through semantic preprocessing, even before semantic checks consume it.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Missing required DSL contract keys are best surfaced from parser post-processing (`parse_at`) instead of relying on generic pest syntax errors.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Extern declarations currently parse into `topology.extern_functions`; call-site parsing (`action: call`) remains for subsequent stories.
---
## [2026-02-27 23:44:07] - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Finalized and added MVP extern-function syntax spec document with supported grammar, out-of-scope list, and 12 valid/invalid DSL examples.
  - Marked US-001 as completed (`passes: true`) in `prd.json`.
- Files changed
  - docs/extern_function_mvp_spec.md
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For PRD-driven work, always update both the story status in `prd.json` and the narrative log in `progress.txt` in the same commit.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `progress.txt` may start without a `## Codebase Patterns` section; add it near the top once reusable patterns are identified.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Extern-function MVP scope is now frozen in `docs/extern_function_mvp_spec.md`; later parser/runtime stories should implement exactly this surface.
---
## [2026-02-28 07:25:15] - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added DSL grammar + parser support for `action: call func(a, b) -> binding` and tuple binding form `-> (a, b, c)`.
  - Added AST action variant (`ActionStatement::Call`) plus return binding representation (`ExternCallBinding`).
  - Added parser guard that rejects extern function usage in expression contexts and points users to `action: call`.
  - Added parser tests for single/tuple extern call actions and expression-context rejection.
  - Marked US-003 as completed (`passes: true`) in `prd.json`.
- Files changed
  - src/ast/mod.rs
  - src/parser/plc.pest
  - src/parser/mod.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Context-sensitive DSL rules are enforced after parse by scanning AST statements/expressions rather than over-constraining pest grammar.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Extern-in-expression checks must recurse into nested statement containers (`repeat`, `parallel`, `race`) to avoid escape paths.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Extern calls now parse into `StepStatement::Action(ActionStatement::Call { ... })`, ready for semantic/runtime handling in follow-up stories.
---
## [2026-02-28 08:18:54] - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Extended IR topology with extern metadata (`ExternFunctionDef` + params/contract) and propagated parser AST extern declarations into `TopologyGraph`.
  - Added IR/runtime action surface for extern invocation (`TransitionAction::CallExtern`) including argument raw expressions and single/tuple bindings.
  - Lowered `ActionStatement::Call` into IR call actions and updated dependent layers (runtime bridge unsupported-action path, ST codegen rendering/variable inference, safety verifier action handling).
  - Added serde/lowering coverage: AST roundtrip test for extern declaration + call action, IR serialization test for extern metadata/call action, and semantic tests for extern metadata + call lowering.
  - Marked US-004 as completed (`passes: true`) in `prd.json`.
- Files changed
  - AGENTS.md
  - src/ast/mod.rs
  - src/ir/mod.rs
  - src/semantic/mod.rs
  - src/runtime_bridge.rs
  - src/codegen/st.rs
  - src/verification/safety.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Extern feature delivery is cross-layer: AST parse success is insufficient unless IR projection + downstream action consumers are updated together.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Single-step tasks without `on_complete` may not emit action transitions, so semantic lowering tests should include an explicit completion target when asserting transition actions.
  - Useful context (e.g., "the evaluation panel is in component X")
    - `TransitionAction::CallExtern` is currently structural only; runtime bridge reports deterministic `UnsupportedAction` until execution support lands in later stories.
---
## [2026-02-28 08:34:52] - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented
  - Added semantic extern signature-table collection with duplicate-name diagnostics during state-machine build.
  - Added compile-time checks for `action: call` argument count/types and return-binding arity/types (single + tuple), including undefined binding targets.
  - Added/updated semantic tests for arg-count mismatch, arg-type mismatch, return arity/type mismatches, duplicate extern names, and line-aware diagnostics.
  - Updated existing extern call lowering test to declare bound variables explicitly.
  - Marked US-005 as completed (`passes: true`) in `prd.json`.
- Files changed
  - AGENTS.md
  - src/semantic/mod.rs
  - prd.json
  - progress.txt
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Extern call validation should run in the same semantic pass as expression-variable checks so arg inference can reuse declared variable types.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Existing extern lowering tests may rely on implicit return targets; after signature checks, tuple/single bindings must reference declared variables.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Duplicate extern function names are now rejected from semantic symbol collection even when no call site references them.
---
