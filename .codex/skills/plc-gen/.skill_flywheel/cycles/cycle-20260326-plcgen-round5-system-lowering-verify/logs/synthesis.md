# 实例聚合

## 总体信号

支持

## 共性问题

- confirmed system -> plc lowering 已成为显式主路径。
- `wafer_loader.system.md` 级别合同不再只被当成 Day-1 脚手架输入，而是被明确映射到 task / service / warning / fault / counter / resource 结构。

## 实例特有问题

- 无。本轮只有 1 个 `weak-blind` 实例。

## 冲突与解释

- 无冲突；剩余限制仅是证据强度和未做真实 end-to-end 生成任务。
