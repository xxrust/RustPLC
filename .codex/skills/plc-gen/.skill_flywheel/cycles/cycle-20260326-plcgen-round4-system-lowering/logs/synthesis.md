# 实例聚合

## 总体信号

支持

## 共性问题

- lowering 缺口集中在 `.system.md` 到 `.plc` 结构映射，而不是 launcher 或 scaffold。
- 新导出的 lowering 工件正对 task、mode、fault、counter、resource 这五类结构。

## 实例特有问题

- 无。本轮只有 1 个 `weak-blind` 实例。

## 冲突与解释

- 无冲突；当前限制仍是单实例弱盲证据强度。
